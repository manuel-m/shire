// Check if client has active engagements via engagement-service
// eslint-disable-next-line sonarjs/todo-tag -- stub until engagement-service exists
// TODO: implement actual call to engagement-service when it exists
export function checkActiveEngagements(_clientId: string): Promise<boolean> {
  return Promise.resolve(false);
}
